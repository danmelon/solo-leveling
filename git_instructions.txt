git init (for new folders outside of current)
git remote add origin <repository location>

    > May need to do this if above doesn't work: git pull origin main --rebase

touch
git status
git pull (combo of git fetch and git merge)
git branch [name]
git checkout [name]
git add -A
git commit -m "some text"
git push origin [branch - usually main for me since I'm solo]

rename using: git mv <old name> <new name>